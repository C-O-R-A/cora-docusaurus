# R105 Assembly Guide

<a className="badge-cad" href="https://raw.githubusercontent.com/C-O-R-A/Build/main/r105_assembly.pdf" download style={{textDecoration: 'none', padding: '6px 12px', borderRadius: '4px', fontFamily: 'var(--mono)', fontSize: '12px'}}>Download Guide</a>

## Build Steps
> TODO: add BOM per build step

1. **Motor and Controller**

   <video autoPlay loop muted playsInline style={{width: '100%', borderRadius: '8px', margin: '16px 0'}}>
     <source src="/assets/videos/1_.webm" type="video/webm" />
   </video>

   Attach the bldc-motor and controller to motor mount, then connect the motor coils to the correct terminals as shown.

   <div>
       <img src="/assets/diagrams/joint_electronics.png" alt="wiring" style={{width: '60%', borderRadius: '6px', marginBottom: '12px'}} />
   </div>

   :::warning
   If you connect the motor coils in a different order, make sure to take note of it if recalibration or disassembly is ever needed.
   :::

2. Run motor calibration.

3. Attach stage 1 planetary gearbox.

   <video autoPlay loop muted playsInline style={{width: '100%', borderRadius: '8px', margin: '16px 0'}}>
     <source src="/assets/videos/2_A.webm" type="video/webm" />
   </video>

   :::note
   Assemble the gearbox such that all the arrows point inward and all the round holes point in the same direction.
   :::

4. Assemble and mount stage 2.

   <video autoPlay loop muted playsInline style={{width: '100%', borderRadius: '8px', margin: '16px 0'}}>
     <source src="/assets/videos/2_B.webm" type="video/webm" />
   </video>